# Javascript Object Reverse Order (npm)

Using:

´´´javascript
import reverse from 'reverse-object-order';

var object = {"one": "a", "two": "b", "three": "c"};

var reversedObject = reverse(object);

console.log(reversedObject);
>>> {"three": "c", "two": "b", "one": "a"}
´´´
